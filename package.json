{
  "name": "tailorcv",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "next build",
    "start": "npm run start --workspace=web",
    "lint": "npm run lint --workspace=web"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
